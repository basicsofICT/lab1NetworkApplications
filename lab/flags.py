# Lab secrets used by the local practice services only.
# Do not copy these into yourAnswers.md by reading this file — that is an honor-code violation.
# Find them through the lab tasks (enumeration and the chatbot).

WEB_FLAG = "WEB_FLAG{r0b0ts_l34d_t0_s3cr3ts}"
LLM_FLAG = "LLM_FLAG{pr0mpt_1nj3ct10n_w1ns}"
INTERNAL_API_KEY = "sk-lab-not-a-real-key-42"
INTERNAL_HOST = "payroll-db.lab.internal"
