{
  "build": { "dockerfile": "Dockerfile" },
  "features": {},
  "forwardPorts": [3000, 8080, 445],
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y whois dnsutils nmap nikto smbclient jq",
  "postStartCommand": "docker compose up -d",
  "customizations": {
    "vscode": { "extensions": ["ms-vscode.remote-repositories"] }
  }
}