{
  "name": "Network Applications & Security - Lab 1",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",

  "forwardPorts": [3000, 8080],
  "portsAttributes": {
    "3000": { "label": "Campus portal", "onAutoForward": "notify" },
    "8080": { "label": "CampusBot", "onAutoForward": "notify" }
  },

  "postCreateCommand": "bash -lc 'set -euxo pipefail; export DEBIAN_FRONTEND=noninteractive; sudo apt-get update -y; sudo apt-get install -y python3 python3-venv python3-pip curl netcat-traditional whois dnsutils nmap nikto jq; chmod +x .devcontainer/*.sh scripts/*.sh || true'",

  "postAttachCommand": "bash .devcontainer/start-services.sh",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.remote-repositories",
        "GitHub.copilot",
        "GitHub.copilot-chat"
      ]
    }
  }
}
