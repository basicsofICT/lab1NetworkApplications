{
"name": "Network Applications & Security - Lab Report: Reconnaissance & Vulnerability Scanning Lab",
"image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",

  "forwardPorts": [3000, 8080],

  "postCreateCommand": "bash -lc 'set -euxo pipefail; export DEBIAN_FRONTEND=noninteractive; sudo apt-get update -y; sudo apt-get install -y python3 python3-venv python3-pip curl netcat-traditional whois dnsutils nmap nikto jq; chmod +x .devcontainer/*.sh || true; chmod +x scripts/*.sh || true'",

  "postAttachCommand": "bash .devcontainer/start-services.sh",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.remote-repositories"
      ]
    }
  }
}